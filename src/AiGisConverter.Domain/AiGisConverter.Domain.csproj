<Project Sdk="Microsoft.NET.Sdk">

  <!-- Layer: Domain. Enterprise entities, value objects and driving/driven ports. No outward references. -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>AiGisConverter.Domain</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="NetTopologySuite" />
  </ItemGroup>

</Project>
